<header id="mainHeader">
    <h3>
        <%= currentNav.title %>
    </h3>
    <h4>
        <%= currentNav.secondaryTitle %>
    </h4>

    <nav>
        <a href="<%= currentNav.navPages[0].link %>">
            <button id="<%= currentNav.navPages[0].id %>">
                <p><%= currentNav.navPages[0].name %></p>
            </button>
        </a>
        
    </nav>
</header>